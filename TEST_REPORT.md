# LumaClip 测试报告

测试日期：2026-07-26  
平台：Windows NT 10.0.26200 x64 / .NET 8.0.29

## 自动化回归

`LumaClip.Tests` 自测结果：6/6 通过。

- 数据库初始化与迁移
- 内容去重与复制计数
- 搜索、收藏、置顶、标签和备注
- 回收站、恢复与永久删除
- 15,000 条历史记录搜索
- Windows 剪贴板文字、图片原图/缩略图、文件列表端到端写入

最近一次 15,000 条记录中的唯一关键词查询耗时约 208 ms。机器可读结果位于 `TestArtifacts\selftest-results.json`。

## 运行态验证

- 主窗口实际启动并截图检查，保留 Windows 原生标题栏、最小化、最大化和关闭行为。
- 设置页实际打开；主题下拉、自定义开关、快捷键输入、隐私、悬浮窗和存储分区均可访问。
- `Ctrl+Alt+V` 实际打开独立悬浮窗；窗口支持置顶、拖动、边缘吸附、折叠、隐藏与调整大小。
- 系统剪贴板实际写入文字、PNG 图片和文件列表后，SQLite、原图和缩略图均生成成功。
- Debug 和 Release 构建均为 0 警告、0 错误。
- 自包含便携版实际启动，第二次启动会退出并唤醒已有实例；单实例验证通过。
- 中文安装程序静默安装返回 0，安装后的 EXE 实际启动成功；卸载返回 0，程序目录被完整移除，用户数据目录保留。
- 关闭主界面、托盘空闲 10 秒期间进程新增 CPU 时间约 0.36 秒；自包含 WPF 发布版测得私有内存约 282 MB、工作集约 443 MB。

## 尚未覆盖

- 没有代码签名证书，因此未验证签名和 SmartScreen 信誉。
- 没有覆盖所有第三方应用的私有剪贴板格式。
- 多显示器位置记忆使用每显示器 DPI 换算，已做边界约束，但未覆盖所有混合缩放组合。
