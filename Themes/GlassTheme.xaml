<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:core="clr-namespace:LumaClip.Core"
                    xmlns:system="clr-namespace:System;assembly=mscorlib">
  <!-- Calm, high-contrast semantic material palette. ThemeManager replaces these at runtime. -->
  <SolidColorBrush x:Key="WindowBackground" Color="#D8EDF1F8"/>
  <SolidColorBrush x:Key="GlassSurface" Color="#BFFFFFFF"/>
  <SolidColorBrush x:Key="GlassSurfaceStrong" Color="#E8FFFFFF"/>
  <SolidColorBrush x:Key="GlassSurfaceSubtle" Color="#8FFFFFFF"/>
  <SolidColorBrush x:Key="GlassHighlightBrush" Color="#E2FFFFFF"/>
  <SolidColorBrush x:Key="SidebarSurface" Color="#78FFFFFF"/>
  <SolidColorBrush x:Key="InspectorSurface" Color="#B8FFFFFF"/>
  <SolidColorBrush x:Key="TileSurface" Color="#96FFFFFF"/>
  <SolidColorBrush x:Key="TileHoverBrush" Color="#B8FFFFFF"/>
  <SolidColorBrush x:Key="SelectedTileBrush" Color="#A8EAF3FF"/>
  <SolidColorBrush x:Key="TileBorderBrush" Color="#A8FFFFFF"/>
  <SolidColorBrush x:Key="SeparatorBrush" Color="#16111820"/>
  <SolidColorBrush x:Key="ImageScrimBrush" Color="#18FFFFFF"/>
  <SolidColorBrush x:Key="SuccessBrush" Color="#FF30D158"/>
  <SolidColorBrush x:Key="BackdropTintBrush" Color="#0AFFFFFF"/>
  <system:Double x:Key="BackdropImageOpacity">1</system:Double>
  <SolidColorBrush x:Key="TextPrimary" Color="#FF17181C"/>
  <SolidColorBrush x:Key="TextSecondary" Color="#FF626771"/>
  <SolidColorBrush x:Key="TextTertiary" Color="#FF9297A1"/>
  <SolidColorBrush x:Key="BorderBrush" Color="#18121720"/>
  <SolidColorBrush x:Key="HoverBrush" Color="#0F101318"/>
  <SolidColorBrush x:Key="SelectedBrush" Color="#22007AFF"/>
  <SolidColorBrush x:Key="InputBrush" Color="#9FFFFFFF"/>
  <SolidColorBrush x:Key="AccentBrush" Color="#FF007AFF"/>
  <SolidColorBrush x:Key="AccentSoftBrush" Color="#1F007AFF"/>
  <SolidColorBrush x:Key="DangerBrush" Color="#FFFF3B30"/>
  <SolidColorBrush x:Key="CodeBrush" Color="#EAF2F3F6"/>
  <SolidColorBrush x:Key="AmbientBlueBrush" Color="#2479BFFF"/>
  <SolidColorBrush x:Key="AmbientVioletBrush" Color="#16A98BFF"/>
  <Color x:Key="AmbientBlueColor">#B896CFFF</Color>
  <Color x:Key="AmbientVioletColor">#9AC1A7FF</Color>

  <core:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
  <core:BoolToVisibilityConverter x:Key="InverseBoolToVisibility" Invert="True"/>
  <core:ImagePathConverter x:Key="ImagePath"/>
  <core:KindEqualsConverter x:Key="KindEquals"/>
  <core:NullToVisibilityConverter x:Key="NullToVisibility"/>
  <core:NullToVisibilityConverter x:Key="NullToVisibilityInverse" Invert="True"/>
  <core:ShelfItemWidthConverter x:Key="ShelfItemWidth"/>
  <core:EmptyStringToVisibilityConverter x:Key="EmptyStringToVisibility"/>

  <DropShadowEffect x:Key="PanelShadow" BlurRadius="30" ShadowDepth="6" Direction="270" Color="#500A1020" Opacity=".16"/>
  <DropShadowEffect x:Key="FloatShadow" BlurRadius="48" ShadowDepth="12" Direction="270" Color="#7006101F" Opacity=".28"/>
  <DropShadowEffect x:Key="SoftShadow" BlurRadius="16" ShadowDepth="3" Direction="270" Color="#38051020" Opacity=".14"/>
  <DropShadowEffect x:Key="InspectorShadow" BlurRadius="42" ShadowDepth="9" Direction="270" Color="#4A071323" Opacity=".2"/>
  <!-- Selection is expressed as physical elevation, not a colored Windows-style outline. -->
  <DropShadowEffect x:Key="SelectedTileShadow" BlurRadius="24" ShadowDepth="7" Direction="270"
                    Color="#7A43516A" Opacity=".28" RenderingBias="Quality"/>

  <Style TargetType="Window">
    <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI"/>
    <Setter Property="FontSize" Value="13"/>
    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
    <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
    <Setter Property="TextOptions.TextRenderingMode" Value="Grayscale"/>
  </Style>

  <Style x:Key="GlassPanel" TargetType="Border">
    <!-- Structural material: a quiet, thicker surface, not a stack of opaque cards. -->
    <Setter Property="Background" Value="{DynamicResource GlassSurface}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource GlassHighlightBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="18"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
  </Style>

  <Style x:Key="GlassSidebar" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource SidebarSurface}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource GlassHighlightBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="20"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
  </Style>

  <Style x:Key="GlassInspector" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource InspectorSurface}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource GlassHighlightBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="22"/>
    <Setter Property="SnapsToDevicePixels" Value="True"/>
  </Style>

  <Style x:Key="PreviewSurface" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource GlassSurfaceStrong}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource GlassHighlightBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="15"/>
    <Setter Property="Margin" Value="0,0,0,12"/>
  </Style>

  <Style x:Key="InspectorGroup" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource GlassSurfaceSubtle}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource TileBorderBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="14"/>
    <Setter Property="Margin" Value="0,0,0,10"/>
  </Style>

  <Style x:Key="LocalDataBadge" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource GlassSurfaceSubtle}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource TileBorderBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="12"/>
    <Setter Property="Padding" Value="10"/>
  </Style>

  <Style x:Key="CountBadge" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}"/>
    <Setter Property="CornerRadius" Value="9"/>
    <Setter Property="Padding" Value="7,2"/>
  </Style>

  <Style x:Key="KeyBadge" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource GlassSurfaceSubtle}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource TileBorderBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="7"/>
    <Setter Property="Padding" Value="7,3"/>
  </Style>

  <Style x:Key="GlassCard" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource GlassSurfaceSubtle}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource GlassHighlightBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="14"/>
    <Setter Property="Padding" Value="14"/>
    <Setter Property="Margin" Value="0,0,0,10"/>
  </Style>

  <!-- History stays visually light; selection, rather than permanent boxes, carries hierarchy. -->
  <Style x:Key="HistoryCard" TargetType="Border">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderBrush" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="14"/>
    <Setter Property="Padding" Value="12"/>
    <Setter Property="Margin" Value="0,0,3,6"/>
  </Style>

  <!-- Press response starts immediately and returns from the presentation value. -->
  <Style TargetType="Button">
    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
    <Setter Property="Background" Value="{DynamicResource GlassSurfaceSubtle}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource GlassHighlightBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="15,8"/>
    <Setter Property="MinHeight" Value="36"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    <Setter Property="HorizontalContentAlignment" Value="Center"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="11" Padding="{TemplateBinding Padding}"
                  RenderTransformOrigin=".5,.5">
            <Border.RenderTransform><ScaleTransform x:Name="PressScale" ScaleX="1" ScaleY="1"/></Border.RenderTransform>
            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"
                              RecognizesAccessKey="True"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HoverBrush}"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Trigger.EnterActions>
                <BeginStoryboard>
                  <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="PressScale" Storyboard.TargetProperty="ScaleX" To=".972" Duration="0:0:0.07"/>
                    <DoubleAnimation Storyboard.TargetName="PressScale" Storyboard.TargetProperty="ScaleY" To=".972" Duration="0:0:0.07"/>
                    <DoubleAnimation Storyboard.TargetName="Bd" Storyboard.TargetProperty="Opacity" To=".84" Duration="0:0:0.07"/>
                  </Storyboard>
                </BeginStoryboard>
              </Trigger.EnterActions>
              <Trigger.ExitActions>
                <BeginStoryboard>
                  <Storyboard>
                    <DoubleAnimation Storyboard.TargetName="PressScale" Storyboard.TargetProperty="ScaleX" To="1" Duration="0:0:0.18">
                      <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation Storyboard.TargetName="PressScale" Storyboard.TargetProperty="ScaleY" To="1" Duration="0:0:0.18">
                      <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation Storyboard.TargetName="Bd" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.18"/>
                  </Storyboard>
                </BeginStoryboard>
              </Trigger.ExitActions>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Bd" Property="Opacity" Value=".38"/></Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
    <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
    <Setter Property="Foreground" Value="White"/>
    <Setter Property="BorderBrush" Value="#40FFFFFF"/>
    <Setter Property="FontWeight" Value="SemiBold"/>
    <Setter Property="Padding" Value="20,10"/>
  </Style>

  <Style x:Key="IconButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
    <Setter Property="Width" Value="36"/>
    <Setter Property="Height" Value="36"/>
    <Setter Property="MinHeight" Value="0"/>
    <Setter Property="Padding" Value="0"/>
    <Setter Property="FontFamily" Value="Segoe Fluent Icons"/>
    <Setter Property="FontSize" Value="14"/>
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="0"/>
  </Style>

  <Style x:Key="FilterChip" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
    <Setter Property="MinHeight" Value="32"/>
    <Setter Property="Padding" Value="14,6"/>
    <Setter Property="Margin" Value="0,0,7,0"/>
    <Setter Property="Background" Value="{DynamicResource GlassSurfaceSubtle}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource TileBorderBrush}"/>
    <Setter Property="FontSize" Value="12"/>
  </Style>

  <Style x:Key="NavButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="HorizontalContentAlignment" Value="Left"/>
    <Setter Property="Padding" Value="11,9"/>
    <Setter Property="Margin" Value="0,2"/>
    <Setter Property="FontWeight" Value="Normal"/>
  </Style>

  <Style x:Key="NavGlyph" TargetType="TextBlock">
    <Setter Property="FontFamily" Value="Segoe Fluent Icons"/>
    <Setter Property="FontSize" Value="14"/>
    <Setter Property="Width" Value="28"/>
    <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
    <Setter Property="VerticalAlignment" Value="Center"/>
  </Style>

  <Style TargetType="TextBox">
    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
    <Setter Property="Background" Value="{DynamicResource InputBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource GlassHighlightBrush}"/>
    <Setter Property="CaretBrush" Value="{DynamicResource AccentBrush}"/>
    <Setter Property="SelectionBrush" Value="{DynamicResource AccentBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="13,9"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TextBox">
          <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12">
            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsKeyboardFocused" Value="True">
              <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource GlassSurfaceStrong}"/>
            </Trigger>
            <Trigger Property="IsReadOnly" Value="True"><Setter TargetName="Bd" Property="Opacity" Value=".92"/></Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="SearchTextBox" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
    <Setter Property="Background" Value="{DynamicResource GlassSurfaceStrong}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource GlassHighlightBrush}"/>
    <Setter Property="FontSize" Value="13"/>
    <Setter Property="Effect" Value="{StaticResource SoftShadow}"/>
  </Style>

  <!-- Apple-style switch control used for settings toggles. -->
  <Style TargetType="CheckBox">
    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
    <Setter Property="Margin" Value="0,6"/>
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="CheckBox">
          <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="38"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Border x:Name="Track" Width="36" Height="22" CornerRadius="11" Background="{DynamicResource InputBrush}"
                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
              <Ellipse x:Name="Knob" Width="18" Height="18" Margin="1" HorizontalAlignment="Left" Fill="White"
                       Effect="{StaticResource SoftShadow}"/>
            </Border>
            <ContentPresenter Grid.Column="2" VerticalAlignment="Center" RecognizesAccessKey="True"/>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="Track" Property="Background" Value="{DynamicResource AccentBrush}"/>
              <Setter TargetName="Track" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
              <Setter TargetName="Knob" Property="HorizontalAlignment" Value="Right"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Track" Property="Opacity" Value=".84"/></Trigger>
            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Track" Property="Opacity" Value=".38"/></Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ComboBoxItem">
    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
    <Setter Property="Padding" Value="12,8"/>
    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBoxItem">
          <Border x:Name="ItemBorder" Background="Transparent" CornerRadius="9" Padding="{TemplateBinding Padding}">
            <ContentPresenter/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsHighlighted" Value="True"><Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource HoverBrush}"/></Trigger>
            <Trigger Property="IsSelected" Value="True"><Setter TargetName="ItemBorder" Property="Background" Value="{DynamicResource SelectedBrush}"/></Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ComboBox">
    <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
    <Setter Property="Background" Value="{DynamicResource InputBrush}"/>
    <Setter Property="BorderBrush" Value="{DynamicResource GlassHighlightBrush}"/>
    <Setter Property="Padding" Value="13,9"/>
    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ComboBox">
          <Grid>
            <ToggleButton x:Name="Toggle" Focusable="False" ClickMode="Press"
                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
              <ToggleButton.Template>
                <ControlTemplate TargetType="ToggleButton">
                  <Border x:Name="ComboBorder" Background="{DynamicResource InputBrush}" BorderBrush="{DynamicResource GlassHighlightBrush}"
                          BorderThickness="1" CornerRadius="12" Padding="{Binding Padding, RelativeSource={RelativeSource AncestorType=ComboBox}}">
                    <Grid>
                      <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="22"/></Grid.ColumnDefinitions>
                      <ContentPresenter Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                        ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                        VerticalAlignment="Center"/>
                      <TextBlock Grid.Column="1" Text="&#xE70D;" FontFamily="Segoe Fluent Icons" FontSize="11"
                                 HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondary}"/>
                    </Grid>
                  </Border>
                  <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ComboBorder" Property="Background" Value="{DynamicResource HoverBrush}"/></Trigger>
                    <Trigger Property="IsChecked" Value="True"><Setter TargetName="ComboBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/></Trigger>
                  </ControlTemplate.Triggers>
                </ControlTemplate>
              </ToggleButton.Template>
            </ToggleButton>
            <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True"
                   PopupAnimation="Fade" Focusable="False">
              <Border Background="{DynamicResource GlassSurfaceStrong}" BorderBrush="{DynamicResource GlassHighlightBrush}"
                      BorderThickness="1" CornerRadius="14" Padding="6" Margin="0,6,0,0" MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                      Effect="{StaticResource PanelShadow}">
                <ScrollViewer MaxHeight="300"><StackPanel IsItemsHost="True"/></ScrollViewer>
              </Border>
            </Popup>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="HasItems" Value="False"><Setter TargetName="PART_Popup" Property="MinHeight" Value="40"/></Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="Slider">
    <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
    <Setter Property="Background" Value="{DynamicResource InputBrush}"/>
    <Setter Property="Height" Value="24"/>
  </Style>

  <Style TargetType="ListBox">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
    <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True"/>
    <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling"/>
  </Style>

  <Style TargetType="ListBoxItem">
    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Padding" Value="0"/>
    <Setter Property="Margin" Value="0"/>
    <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ListBoxItem">
          <Border x:Name="Selection" Background="{TemplateBinding Background}" CornerRadius="17" Padding="3">
            <ContentPresenter/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Selection" Property="Background" Value="{DynamicResource HoverBrush}"/></Trigger>
            <Trigger Property="IsSelected" Value="True"><Setter TargetName="Selection" Property="Background" Value="{DynamicResource SelectedBrush}"/></Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style TargetType="ScrollBar">
    <Setter Property="Width" Value="6"/>
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="Opacity" Value=".48"/>
  </Style>

  <Style x:Key="SectionTitle" TargetType="TextBlock">
    <Setter Property="FontSize" Value="11"/>
    <Setter Property="FontWeight" Value="SemiBold"/>
    <Setter Property="Foreground" Value="{DynamicResource TextTertiary}"/>
    <Setter Property="Margin" Value="11,16,0,6"/>
  </Style>
  <Style x:Key="Caption" TargetType="TextBlock">
    <Setter Property="FontSize" Value="12"/>
    <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
    <Setter Property="LineHeight" Value="17"/>
  </Style>
</ResourceDictionary>
